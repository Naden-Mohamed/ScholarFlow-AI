from enum import Enum

class DataBaseEnums(Enum):

    PROJECTS_COLLECTION = "projects"
    DATABASE_NAME = "AI_Agents"
    DATA_CHUNKS_COLLECTION = "data_chunks"
    ASSET_COLLECTION = "assets" # Store each collection of related assets in a separated project collection